{
  "scripts": {
    "start": "snowpack dev",
    "build": "snowpack build",
    "test": "set -x; cd ..; sbtn example/test; cd -"
  },
  "dependencies": {
    "rxjs": "^6.6.3"
  },
  "devDependencies": {
    "@snowpack/plugin-run-script": "^2.1.1",
    "@types/snowpack-env": "^2.3.0",
    "snowpack": "^2.10.3",
    "typescript": "^4.0.2"
  }
}
